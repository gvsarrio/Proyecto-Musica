{% extends 'base.html.twig' %}

{% block body %}

    <h1>Nuevo mensaje</h1>

    <p>
        Destinatario:
        {{ destinatario.musico.nombre }}
    </p>

    {{ form_start(form) }}

        {{ form_row(form.contenido) }}

        <button type="submit">
            Enviar
        </button>

    {{ form_end(form) }}

{% endblock %}